# 测试问题集（共 25 条）

**说明**：
- 所有参考 SQL 基于数据集真实表结构（customer / product / date / order_item）
- 统一默认过滤 `pay_status = '已支付'`
- 所有时间范围以"假设当前日期 = 2026-07-01"为基准解析（与数据集截止 2026-06-30 匹配）

---

## 一、直接统计类（5 条）

### Q01
**问题**：上月销售额多少？
- **指标**：M01 销售额
- **维度**：无
- **时间范围**：2026-06-01 ~ 2026-06-30（完整上月，因"今天"假设为 2026-07-01）
- **问题类型**：直接统计
- **难度**：简单
- **口径风险**：容易把已退款/未支付算进去；或者算成近30天而不是完整自然月

**参考 SQL**：
```sql
SELECT ROUND(SUM(amount), 2) AS sales_amount
FROM order_item
WHERE pay_status = '已支付'
  AND order_date BETWEEN '2026-06-01' AND '2026-06-30';
```

---

### Q02
**问题**：今年上半年华东区域的销售额是多少？
- **指标**：M01 销售额
- **维度**：region = 华东
- **时间范围**：2026-01-01 ~ 2026-06-30
- **问题类型**：直接统计
- **难度**：中等（需 2 表 join）

**参考 SQL**：
```sql
SELECT ROUND(SUM(oi.amount), 2) AS sales_amount
FROM order_item oi
LEFT JOIN customer c ON oi.customer_id = c.customer_id
WHERE oi.pay_status = '已支付'
  AND c.region = '华东'
  AND oi.order_date BETWEEN '2026-01-01' AND '2026-06-30';
```

---

### Q03
**问题**：2025 年 Q4 电子产品的销售额是多少？
- **指标**：M01 销售额
- **维度**：category = 电子产品
- **时间范围**：2025-10-01 ~ 2025-12-31（2025Q4）
- **问题类型**：直接统计
- **难度**：中等

**参考 SQL**：
```sql
SELECT ROUND(SUM(oi.amount), 2) AS sales_amount
FROM order_item oi
LEFT JOIN product p ON oi.product_id = p.product_id
WHERE oi.pay_status = '已支付'
  AND p.category = '电子产品'
  AND oi.order_date BETWEEN '2025-10-01' AND '2025-12-31';
```

---

### Q04
**问题**：去年金卡会员的订单量是多少？
- **指标**：M02 订单量
- **维度**：member_level = 金卡
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：直接统计
- **难度**：中等
- **口径风险**：忘记 DISTINCT order_id，按行数统计导致虚高

**参考 SQL**：
```sql
SELECT COUNT(DISTINCT oi.order_id) AS order_count
FROM order_item oi
LEFT JOIN customer c ON oi.customer_id = c.customer_id
WHERE oi.pay_status = '已支付'
  AND c.member_level = '金卡'
  AND oi.order_date BETWEEN '2025-01-01' AND '2025-12-31';
```

---

### Q05
**问题**：2025 年全年的客单价是多少？
- **指标**：M03 客单价
- **维度**：无
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：直接统计
- **难度**：中等
- **口径风险**：分母误用 COUNT(*) 行数或客户数

**参考 SQL**：
```sql
SELECT ROUND(
         SUM(amount) * 1.0 / NULLIF(COUNT(DISTINCT order_id), 0),
         2
       ) AS aov
FROM order_item
WHERE pay_status = '已支付'
  AND order_date BETWEEN '2025-01-01' AND '2025-12-31';
```

---

## 二、趋势分析类（5 条）

### Q06
**问题**：2025 年每个月的销售额变化趋势是怎样的？
- **指标**：M01 销售额
- **维度**：月份
- **时间范围**：2025-01 ~ 2025-12，按月分组
- **问题类型**：趋势分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT strftime('%Y-%m', order_date) AS month,
       ROUND(SUM(amount), 2)          AS sales_amount
FROM order_item
WHERE pay_status = '已支付'
  AND order_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY month
ORDER BY month;
```
*注：SQLite 日期函数用 strftime；PostgreSQL 用 to_char(order_date, 'YYYY-MM')*

---

### Q07
**问题**：近 6 个月的订单量变化趋势
- **指标**：M02 订单量
- **维度**：月份
- **时间范围**：2026-01 ~ 2026-06（近6个完整月，以 2026-07-01 为基准）
- **问题类型**：趋势分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT strftime('%Y-%m', order_date) AS month,
       COUNT(DISTINCT order_id)      AS order_count
FROM order_item
WHERE pay_status = '已支付'
  AND order_date BETWEEN '2026-01-01' AND '2026-06-30'
GROUP BY month
ORDER BY month;
```

---

### Q08
**问题**：2025 年各季度新客数的变化情况
- **指标**：M04 新客数
- **维度**：季度
- **时间范围**：2025Q1 ~ 2025Q4（按客户首单时间落在季度内判定）
- **问题类型**：趋势分析
- **难度**：困难（需子查询算首单）
- **口径风险**：按注册日期算新客；按客户注册时间和下单时间交集算

**参考 SQL**：
```sql
WITH first_pay AS (
  SELECT customer_id, MIN(order_date) AS first_date
  FROM order_item
  WHERE pay_status = '已支付'
  GROUP BY customer_id
)
SELECT CASE
         WHEN first_date BETWEEN '2025-01-01' AND '2025-03-31' THEN '2025Q1'
         WHEN first_date BETWEEN '2025-04-01' AND '2025-06-30' THEN '2025Q2'
         WHEN first_date BETWEEN '2025-07-01' AND '2025-09-30' THEN '2025Q3'
         WHEN first_date BETWEEN '2025-10-01' AND '2025-12-31' THEN '2025Q4'
       END AS quarter_name,
       COUNT(DISTINCT customer_id) AS new_customer_count
FROM first_pay
WHERE first_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY quarter_name
ORDER BY quarter_name;
```

---

### Q09
**问题**：2025 年每个月的客单价，同时给出同比上月的环比变化
- **指标**：M03 客单价（含环比）
- **维度**：月份
- **时间范围**：2025-01 ~ 2025-12
- **问题类型**：趋势分析（环比）
- **难度**：困难（窗口函数 LAG）

**参考 SQL**：
```sql
WITH monthly_aov AS (
  SELECT strftime('%Y-%m', order_date)                       AS month,
         ROUND(SUM(amount) * 1.0 / NULLIF(COUNT(DISTINCT order_id), 0), 2) AS aov
  FROM order_item
  WHERE pay_status = '已支付'
    AND order_date BETWEEN '2025-01-01' AND '2025-12-31'
  GROUP BY month
)
SELECT month,
       aov,
       LAG(aov) OVER (ORDER BY month)                         AS prev_aov,
       ROUND((aov - LAG(aov) OVER (ORDER BY month)) * 100.0
             / NULLIF(LAG(aov) OVER (ORDER BY month), 0), 2)  AS mom_change_pct
FROM monthly_aov
ORDER BY month;
```

---

### Q10
**问题**：各渠道（线上APP/线下门店/小程序/第三方平台）按季度的销售额变化趋势
- **指标**：M01 销售额
- **维度**：渠道 × 季度
- **时间范围**：2025Q1 ~ 2026Q2（全部数据）
- **问题类型**：趋势分析（多维交叉）
- **难度**：困难

**参考 SQL**：
```sql
SELECT channel,
       CASE
         WHEN order_date BETWEEN '2025-01-01' AND '2025-03-31' THEN '2025Q1'
         WHEN order_date BETWEEN '2025-04-01' AND '2025-06-30' THEN '2025Q2'
         WHEN order_date BETWEEN '2025-07-01' AND '2025-09-30' THEN '2025Q3'
         WHEN order_date BETWEEN '2025-10-01' AND '2025-12-31' THEN '2025Q4'
         WHEN order_date BETWEEN '2026-01-01' AND '2026-03-31' THEN '2026Q1'
         WHEN order_date BETWEEN '2026-04-01' AND '2026-06-30' THEN '2026Q2'
       END AS quarter_name,
       ROUND(SUM(amount), 2) AS sales_amount
FROM order_item
WHERE pay_status = '已支付'
  AND order_date BETWEEN '2025-01-01' AND '2026-06-30'
GROUP BY channel, quarter_name
ORDER BY channel, quarter_name;
```

---

## 三、对比分析类（5 条）

### Q11
**问题**：华东和华北 2025 年全年的销售额对比
- **指标**：M01 销售额
- **维度**：region（华东 vs 华北）
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：对比分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT c.region,
       ROUND(SUM(oi.amount), 2) AS sales_amount
FROM order_item oi
LEFT JOIN customer c ON oi.customer_id = c.customer_id
WHERE oi.pay_status = '已支付'
  AND c.region IN ('华东', '华北')
  AND oi.order_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY c.region
ORDER BY sales_amount DESC;
```

---

### Q12
**问题**：金卡会员和普通会员在 2025 年 Q4 的订单量对比
- **指标**：M02 订单量
- **维度**：member_level（金卡 vs 普通）
- **时间范围**：2025-10-01 ~ 2025-12-31
- **问题类型**：对比分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT c.member_level,
       COUNT(DISTINCT oi.order_id) AS order_count
FROM order_item oi
LEFT JOIN customer c ON oi.customer_id = c.customer_id
WHERE oi.pay_status = '已支付'
  AND c.member_level IN ('金卡', '普通')
  AND oi.order_date BETWEEN '2025-10-01' AND '2025-12-31'
GROUP BY c.member_level;
```

---

### Q13
**问题**：线上 APP 渠道和线下门店渠道，哪个客单价更高？
- **指标**：M03 客单价
- **维度**：channel（线上APP vs 线下门店）
- **时间范围**：2026-06-01 ~ 2026-06-30（默认上月）
- **问题类型**：对比分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT channel,
       ROUND(SUM(amount) * 1.0 / NULLIF(COUNT(DISTINCT order_id), 0), 2) AS aov
FROM order_item
WHERE pay_status = '已支付'
  AND channel IN ('线上APP', '线下门店')
  AND order_date BETWEEN '2026-06-01' AND '2026-06-30'
GROUP BY channel
ORDER BY aov DESC;
```

---

### Q14
**问题**：2025 年全年 vs 2026 年上半年的销售额对比
- **指标**：M01 销售额
- **维度**：时间段（2025全年 vs 2026H1）
- **时间范围**：两段对比
- **问题类型**：对比分析（跨时段）
- **难度**：中等

**参考 SQL**：
```sql
SELECT CASE
         WHEN order_date BETWEEN '2025-01-01' AND '2025-12-31' THEN '2025全年'
         WHEN order_date BETWEEN '2026-01-01' AND '2026-06-30' THEN '2026上半年'
       END AS period_name,
       ROUND(SUM(amount), 2) AS sales_amount
FROM order_item
WHERE pay_status = '已支付'
  AND (
    order_date BETWEEN '2025-01-01' AND '2025-12-31'
    OR order_date BETWEEN '2026-01-01' AND '2026-06-30'
  )
GROUP BY period_name;
```

---

### Q15
**问题**：五大一级品类 2025 年全年销售额对比
- **指标**：M01 销售额
- **维度**：category（5 个一级品类）
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：对比分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT p.category,
       ROUND(SUM(oi.amount), 2) AS sales_amount
FROM order_item oi
LEFT JOIN product p ON oi.product_id = p.product_id
WHERE oi.pay_status = '已支付'
  AND oi.order_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY p.category
ORDER BY sales_amount DESC;
```

---

## 四、排名分析类（5 条）

### Q16
**问题**：一级品类中，销售额最高的 Top 3 是哪些？（2025 全年）
- **指标**：M01 销售额
- **维度**：category，取 Top 3
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：排名分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT p.category,
       ROUND(SUM(oi.amount), 2) AS sales_amount
FROM order_item oi
LEFT JOIN product p ON oi.product_id = p.product_id
WHERE oi.pay_status = '已支付'
  AND oi.order_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY p.category
ORDER BY sales_amount DESC
LIMIT 3;
```

---

### Q17
**问题**：2026 年上半年订单量最多的前 5 个城市是哪些？
- **指标**：M02 订单量
- **维度**：city，Top 5
- **时间范围**：2026-01-01 ~ 2026-06-30
- **问题类型**：排名分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT c.city,
       COUNT(DISTINCT oi.order_id) AS order_count
FROM order_item oi
LEFT JOIN customer c ON oi.customer_id = c.customer_id
WHERE oi.pay_status = '已支付'
  AND oi.order_date BETWEEN '2026-01-01' AND '2026-06-30'
GROUP BY c.city
ORDER BY order_count DESC
LIMIT 5;
```

---

### Q18
**问题**：按区域来看，客单价最高的 3 个区域是？
- **指标**：M03 客单价
- **维度**：region，Top 3
- **时间范围**：2026-06-01 ~ 2026-06-30（默认上月）
- **问题类型**：排名分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT c.region,
       ROUND(SUM(oi.amount) * 1.0 / NULLIF(COUNT(DISTINCT oi.order_id), 0), 2) AS aov
FROM order_item oi
LEFT JOIN customer c ON oi.customer_id = c.customer_id
WHERE oi.pay_status = '已支付'
  AND oi.order_date BETWEEN '2026-06-01' AND '2026-06-30'
GROUP BY c.region
ORDER BY aov DESC
LIMIT 3;
```

---

### Q19
**问题**：2025 年新增付费客户数最多的 10 个城市
- **指标**：M04 新客数
- **维度**：city，Top 10
- **时间范围**：2025-01-01 ~ 2025-12-31（客户首单时间在此范围）
- **问题类型**：排名分析
- **难度**：困难（新客口径需要子查询）

**参考 SQL**：
```sql
WITH first_pay AS (
  SELECT customer_id, MIN(order_date) AS first_date
  FROM order_item
  WHERE pay_status = '已支付'
  GROUP BY customer_id
)
SELECT c.city,
       COUNT(DISTINCT fp.customer_id) AS new_customer_count
FROM first_pay fp
LEFT JOIN customer c ON fp.customer_id = c.customer_id
WHERE fp.first_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY c.city
ORDER BY new_customer_count DESC
LIMIT 10;
```

---

### Q20
**问题**：2025 年销售额最低的 5 个品牌
- **指标**：M01 销售额
- **维度**：brand，Bottom 5
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：排名分析
- **难度**：中等

**参考 SQL**：
```sql
SELECT p.brand,
       ROUND(SUM(oi.amount), 2) AS sales_amount
FROM order_item oi
LEFT JOIN product p ON oi.product_id = p.product_id
WHERE oi.pay_status = '已支付'
  AND oi.order_date BETWEEN '2025-01-01' AND '2025-12-31'
GROUP BY p.brand
ORDER BY sales_amount ASC
LIMIT 5;
```

---

## 五、占比分析类（3 条）

### Q21
**问题**：7 大区域的销售额占比（上月）
- **指标**：M01 销售额 + M08 区域销售占比
- **维度**：region 全部区域合计 100%
- **时间范围**：2026-06-01 ~ 2026-06-30
- **问题类型**：占比分析
- **难度**：中等

**参考 SQL**：
```sql
WITH sales_by_region AS (
  SELECT c.region,
         SUM(oi.amount) AS total_amount
  FROM order_item oi
  LEFT JOIN customer c ON oi.customer_id = c.customer_id
  WHERE oi.pay_status = '已支付'
    AND oi.order_date BETWEEN '2026-06-01' AND '2026-06-30'
  GROUP BY c.region
),
total AS (SELECT SUM(total_amount) AS all_amount FROM sales_by_region)
SELECT s.region,
       ROUND(s.total_amount, 2)                             AS sales_amount,
       ROUND(s.total_amount * 100.0 / t.all_amount, 2)       AS share_pct
FROM sales_by_region s, total t
ORDER BY share_pct DESC;
```

---

### Q22
**问题**：2025 年各渠道的订单量占比
- **指标**：M02 订单量（占比）
- **维度**：channel
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：占比分析
- **难度**：中等

**参考 SQL**：
```sql
WITH orders_by_channel AS (
  SELECT channel,
         COUNT(DISTINCT order_id) AS order_cnt
  FROM order_item
  WHERE pay_status = '已支付'
    AND order_date BETWEEN '2025-01-01' AND '2025-12-31'
  GROUP BY channel
),
total AS (SELECT SUM(order_cnt) AS all_cnt FROM orders_by_channel)
SELECT o.channel,
       o.order_cnt                                                    AS order_count,
       ROUND(o.order_cnt * 100.0 / t.all_cnt, 2)                      AS share_pct
FROM orders_by_channel o, total t
ORDER BY share_pct DESC;
```

---

### Q23
**问题**："电子产品" 这一一级品类下，5 个二级子品类的销售额占比（2025 全年）
- **指标**：M01 销售额（品类内占比）
- **维度**：sub_category（仅 category=电子产品 内的子品类）
- **时间范围**：2025-01-01 ~ 2025-12-31
- **问题类型**：占比分析
- **难度**：中等

**参考 SQL**：
```sql
WITH sub_sales AS (
  SELECT p.sub_category,
         SUM(oi.amount) AS total_amount
  FROM order_item oi
  LEFT JOIN product p ON oi.product_id = p.product_id
  WHERE oi.pay_status = '已支付'
    AND p.category = '电子产品'
    AND oi.order_date BETWEEN '2025-01-01' AND '2025-12-31'
  GROUP BY p.sub_category
),
total AS (SELECT SUM(total_amount) AS all_amount FROM sub_sales)
SELECT s.sub_category,
       ROUND(s.total_amount, 2)                        AS sales_amount,
       ROUND(s.total_amount * 100.0 / t.all_amount, 2) AS share_pct
FROM sub_sales s, total t
ORDER BY share_pct DESC;
```

---

## 六、异常识别类（2 条）

### Q24
**问题**：2025 年哪个月的销售额环比上月跌幅最大？
- **指标**：M01 销售额（环比跌幅）
- **维度**：月份
- **时间范围**：2025-01 ~ 2025-12，计算相邻月份环比
- **问题类型**：异常识别
- **难度**：困难（窗口函数 + 排序取最值）

**参考 SQL**：
```sql
WITH monthly_sales AS (
  SELECT strftime('%Y-%m', order_date) AS month,
         SUM(amount)                   AS sales_amount
  FROM order_item
  WHERE pay_status = '已支付'
    AND order_date BETWEEN '2025-01-01' AND '2025-12-31'
  GROUP BY month
),
with_mom AS (
  SELECT month,
         sales_amount,
         LAG(sales_amount) OVER (ORDER BY month)                AS prev_sales,
         ROUND((sales_amount - LAG(sales_amount) OVER (ORDER BY month)) * 100.0
               / NULLIF(LAG(sales_amount) OVER (ORDER BY month), 0), 2) AS mom_pct
  FROM monthly_sales
)
SELECT month,
       ROUND(sales_amount, 2) AS sales_amount,
       ROUND(prev_sales, 2)   AS prev_month_sales,
       mom_pct                AS mom_change_pct
FROM with_mom
WHERE mom_pct IS NOT NULL
ORDER BY mom_pct ASC
LIMIT 1;
```

---

### Q25
**问题**：2025 年复购率最高和最低的月份分别是？
- **指标**：M05 复购率
- **维度**：月份
- **时间范围**：2025-01 ~ 2025-12
- **问题类型**：异常识别（极值对比）
- **难度**：困难（复购率口径复杂，客户维度的订单数 ≥ 2）
- **口径风险**：误用订单维度算 ≥ 2；分子分母混淆

**参考 SQL**：
```sql
WITH monthly_customer_orders AS (
  SELECT strftime('%Y-%m', order_date) AS month,
         customer_id,
         COUNT(DISTINCT order_id)      AS order_cnt
  FROM order_item
  WHERE pay_status = '已支付'
    AND order_date BETWEEN '2025-01-01' AND '2025-12-31'
  GROUP BY month, customer_id
),
monthly_repurchase AS (
  SELECT month,
         COUNT(*)                                            AS total_active_customers,
         SUM(CASE WHEN order_cnt >= 2 THEN 1 ELSE 0 END)    AS repurchase_customers
  FROM monthly_customer_orders
  GROUP BY month
)
SELECT month,
       total_active_customers,
       repurchase_customers,
       ROUND(repurchase_customers * 100.0
             / NULLIF(total_active_customers, 0), 2)        AS repurchase_rate_pct
FROM monthly_repurchase
ORDER BY repurchase_rate_pct DESC;
-- 最高：ORDER BY ... DESC LIMIT 1
-- 最低：ORDER BY ... ASC  LIMIT 1
```
