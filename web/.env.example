# ============================================
# 前端环境变量模板（Vite，仅 VITE_ 前缀会被打包进浏览器）
# 使用：cp .env.example .env，按需修改
#
# ⚠️ 安全约束：
#   - 这里【严禁】放置任何密钥（LLM Key、风神 BI Token / JWT / app_secret 等），
#     Vite 构建会把 VITE_ 变量明文打进前端产物；
#   - 所有敏感配置只允许放在项目根目录 .env（后端读取，见根目录 .env.example）。
# ============================================

# 后端 API 地址：
#   - 本地开发留空即可：Vite 开发服务器自动把 /api 代理到 http://localhost:5001
#     （代理规则见 vite.config.ts）
#   - 若后端部署在其他地址/端口，显式指定完整地址，例如：
# VITE_BACKEND_URL=http://localhost:5001
#   - Docker / 生产部署：前后端同域，nginx 将 /api 反向代理到后端，无需设置。

# 离线演示开关（排障用）：
#   不设置或设为 0：前端正常调用后端，后端不可达时才自动降级前端 Mock；
#   设为 1：完全不请求后端，直接使用内置演示数据（纯前端离线演示）。
# VITE_DISABLE_BACKEND=0
