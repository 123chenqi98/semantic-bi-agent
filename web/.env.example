# 前端环境变量（可选）
# 后端默认通过 Vite 代理 /api -> http://localhost:5001
# 如果后端跑在其他端口/地址，可改下面：
# VITE_BACKEND_URL=http://localhost:5001

# 不设置 VITE_DISABLE_BACKEND=1 时：前端先尝试 /api/chat，失败自动回退 Mock
# VITE_DISABLE_BACKEND=1
